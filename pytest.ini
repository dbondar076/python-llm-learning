[pytest]
log_cli = true
log_cli_level = INFO
markers =
    debug: local debug test, not for regular runs
    fast: marks fast unit-style tests
    integration: marks slower integration tests
    mock_only: marks tests that require mock/flaky LLM mode
    real_llm: tests that call the real OpenAI API
asyncio_mode = auto